# Leave VITE_API_BASE_URL unset for same-origin production deployments.
# If the API is hosted separately, set it to the full HTTPS /api origin.
# Example:
# VITE_API_BASE_URL=https://api.flo-state.in/api
