# FlowState Resume Checkpoint

Last checkpoint update: 2026-03-13 01:00:55 +05:30
Purpose: Resume from this exact project state, not from zero.

## 1) Resume Snapshot (Current State)

- Phase status:
  - Phase 1.1, 1.2, 1.3: complete
  - Phase 2.1, 2.2, 2.3: complete
  - Phase 3.1, 3.2, 3.3: complete (dnd-kit for cards)
  - Phase 4: in progress
  - Phase 4.1 (Checklists): implemented on server + client

- Phase 4.1 implementation highlights:
  - Server schema: checklists + checklist_items tables with relations and test DB setup
  - Server API: CRUD for checklists + items in `boards.route.ts`/`boards.service.ts`
  - Client types + API: checklist support in `types/board.ts` and `boards-api.ts`
  - UI: checklist preview on cards + full checklist CRUD in card modal

- Recent UI/UX work:
  - Card editor modal is scrollable with max height
  - Sticky header in card modal with `Delete`, `Close`, `Save changes` actions
  - Checklist items can be toggled directly in card previews
  - Clicking checklist body (expanded) opens card editor + autoscrolls to that checklist
  - `Esc` closes the card editor
  - Header delete button now has `aria-label="Delete card"` to satisfy tests
  - User confirmed all tests pass and checklist body click -> editor + autoscroll works

- Test location change:
  - Client tests moved from `client/src/...` to `client/tests/...`
  - Added `client/tsconfig.test.json`
  - Updated `client/vitest.config.ts` include globs
  - Updated `client/package.json` lint to include tests

## 2) Resume-Next Priority

- Phase 4.2: Attachments (upload + metadata + delete + modal UI).
- Phase 4.3: Labels + Assignments + Cover Colors (board-level CRUD, assign to cards, filter/visuals).

## 3) User Preferences to Preserve on Resume

- Package manager: Bun
- Language: TypeScript for client and server
- Monorepo style: plain workspaces (`client/`, `server/`)
- Assistant writes tests but user runs tests
- Do not run tests unless user explicitly asks
- Test cadence: after full phases (or every 2 full phases), not each subphase
- Progress updates must be recorded in `Docs/context.md` at end of implementation cycles
- Keep in-app confirmations (no browser native confirms)
- Autosave UX for board/list settings is expected

## 4) How to Read and Update Context Files on Resume

Primary resume flow:
1. Read `Docs/context.md` first:
   - Check Progress Tracker table
   - Check Key Decisions
   - Check latest rows in Updates
2. Read `Docs/instructions.md` second:
   - Check active phase checklist
   - Confirm user-run commands section
3. Implement next scoped work
4. Update both files after implementation:
   - `Docs/context.md`: Progress Tracker + Updates row(s)
   - `Docs/instructions.md`: checklist checkboxes/status

Rule: treat `Docs/context.md` as source of truth for project state; treat `Docs/instructions.md` as execution checklist.

## 5) Working Rules I Must Remember on Resume

Code/workflow rules:
- Do not revert unrelated user changes.
- Avoid destructive git commands unless explicitly requested.
- Prefer `rg` for searching.
- Use `apply_patch` for focused file edits when possible.
- Keep edits ASCII unless file already requires Unicode.
- If unexpected file changes appear during work, pause and ask user how to proceed.

Communication rules:
- Be concise, collaborative, and transparent.
- Give short progress updates while working.
- Provide exact commands when user needs to run checks.

## 6) Current Git Working Tree Snapshot (for Resume Awareness)

Tracked modified files at checkpoint:
- `Docs/context.md`
- `Docs/instructions.md`
- `Docs/resume.txt`
- `client/package.json`
- `client/src/lib/boards-api.ts`
- `client/src/pages/boards/board-detail-page.tsx`
- `client/src/types/board.ts`
- `client/tsconfig.json`
- `client/vitest.config.ts`
- `server/src/db/init.ts`
- `server/src/db/schema.ts`
- `server/src/modules/boards/boards.route.ts`
- `server/src/modules/boards/boards.schema.ts`
- `server/src/modules/boards/boards.service.ts`
- DB runtime artifacts (test db shm/wal files)

Tracked deleted files at checkpoint:
- `client/src/pages/home-page.test.tsx`
- `client/src/pages/boards/board-detail-page.test.tsx`

Untracked files at checkpoint:
- `client/tests/`
- `client/tsconfig.test.json`
- `server/tests/checklists.test.ts`
- `server/data/flowstate.checklists.test.db` (+ shm/wal)

Note: db `-shm` and `-wal` files are runtime artifacts; do not delete without user intent.

## 7) Known Resume Risks / Attention Points

- Card modal UI recently restructured (sticky header + scrollable content). Verify actions remain accessible.
- Checklist body click -> editor + scroll relies on `openCardEditor(card, checklistId)`; avoid clearing scroll target prematurely.
- Tests are now in `client/tests/`; ensure tooling/lint includes that path.

## 8) Immediate User-Facing Follow-up Item

- Start Phase 4.2: Attachments (server + client + UI).

## 9) Resume Command Pack

- Client lint:
  - `bun run --cwd client lint`
- Client typecheck:
  - `bunx tsc -b client/tsconfig.json`
- User-run tests:
  - `bun run --cwd client test`
  - `bun run --cwd server test`

This file is the resume checkpoint contract for continuing FlowState safely and quickly.
